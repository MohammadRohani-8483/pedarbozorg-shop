export type SearchValueProps = {
    image: string | undefined;
    name: string;
    link: string;
}

export type comment = {
    name: string,
    score: number,
    product: string,
    comment: string,
    like: number,
    dislike: number,
}

export type groupProduct = {
    link: string,
    icon: string,
    name: string,
}

export type media = {
    name: string,
    link: string,
    icon: string,
    iconHover: string,
}